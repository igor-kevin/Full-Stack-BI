export interface Kpis {
  media: number;
  media_presenca: number;
}

export interface ScatterPoint {
  x: number;
  y: number;
}

export interface BarData {
    nota: string;
    quantidade: number;
}